# ============================================================================
# 🎮 RETRO GAMING / CYBERPUNK STARSHIP THEME 🎮
# ============================================================================
# Inspired by: Cyberpunk 2077, Tron, Synthwave, Retro Gaming
# Color scheme: Neon Pink, Cyan, Purple, Green on dark background
# ============================================================================

# Timeout for commands (in milliseconds)
command_timeout = 1000

# Add blank line between prompts for better readability
add_newline = true

# ============================================================================
# PROMPT FORMAT - The main layout
# ============================================================================
format = """
[┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓](bold cyan)\
[┃](bold cyan) $username$hostname$directory$git_branch$git_status\
[┃](bold cyan)
[┃](bold cyan) $nodejs$python$rust$golang$docker_context$kubernetes\
[┃](bold cyan)
[┗━](bold cyan)$character"""

# Alternative minimal format (uncomment to use)
# format = \"\"\"
# [▓▒░](bold magenta)$directory$git_branch$git_status
# [▓▒░](bold magenta)$character\"\"\"

# ============================================================================
# PROMPT CHARACTER - The input indicator
# ============================================================================
[character]
success_symbol = "[❯❯❯](bold cyan)"
error_symbol = "[❯❯❯](bold red)"
vimcmd_symbol = "[❮❮❮](bold green)"

# ============================================================================
# USERNAME - Show current user
# ============================================================================
[username]
style_user = "bold cyan"
style_root = "bold red"
format = "[$user](bold magenta)[@](bold cyan)"
disabled = false
show_always = true

# ============================================================================
# HOSTNAME - Show machine name
# ============================================================================
[hostname]
ssh_only = false
format = "[$hostname](bold cyan) "
disabled = false

# ============================================================================
# DIRECTORY - Current path
# ============================================================================
[directory]
truncation_length = 4
truncation_symbol = "…/"
home_symbol = "🏠"
read_only = " 🔒"
style = "bold yellow"
format = "in [$path]($style)[$read_only]($read_only_style) "

[directory.substitutions]
"Documents" = "📄"
"Downloads" = "📥"
"Music" = "🎵"
"Pictures" = "🖼️"
"code" = "💻"
"projects" = "🚀"

# ============================================================================
# GIT - Version control
# ============================================================================
[git_branch]
symbol = "🎮 "
format = "on [$symbol$branch(:$remote_branch)]($style) "
style = "bold magenta"
truncation_length = 20
truncation_symbol = "…"

[git_status]
format = '([\[$all_status$ahead_behind\]]($style) )'
style = "bold red"
conflicted = "⚔️ "
ahead = "⇡${count} "
behind = "⇣${count} "
diverged = "⇕⇡${ahead_count}⇣${behind_count} "
up_to_date = "✓ "
untracked = "🆕${count} "
stashed = "📦${count} "
modified = "📝${count} "
staged = "✨${count} "
renamed = "📛${count} "
deleted = "🗑️${count} "

[git_commit]
commit_hash_length = 7
format = "[\\($hash$tag\\)]($style) "
style = "bold green"
tag_symbol = "🏷️ "
tag_disabled = false

# ============================================================================
# PROGRAMMING LANGUAGES
# ============================================================================

[nodejs]
symbol = "⬢ "
format = "via [$symbol($version )]($style)"
style = "bold green"
detect_extensions = ["js", "mjs", "cjs", "ts", "mts", "cts"]
detect_files = ["package.json", ".node-version", ".nvmrc"]
detect_folders = ["node_modules"]

[python]
symbol = "🐍 "
format = 'via [$symbol$pyenv_prefix($version )(\($virtualenv\) )]($style)'
style = "bold yellow"
pyenv_version_name = false
detect_extensions = ["py"]
detect_files = [".python-version", "Pipfile", "pyproject.toml", "requirements.txt"]

[rust]
symbol = "🦀 "
format = "via [$symbol($version )]($style)"
style = "bold red"
detect_extensions = ["rs"]
detect_files = ["Cargo.toml"]

[golang]
symbol = "🐹 "
format = "via [$symbol($version )]($style)"
style = "bold cyan"
detect_extensions = ["go"]
detect_files = ["go.mod", "go.sum"]

[java]
symbol = "☕ "
format = "via [$symbol($version )]($style)"
style = "bold red"

[php]
symbol = "🐘 "
format = "via [$symbol($version )]($style)"
style = "bold purple"

# ============================================================================
# TOOLS & PLATFORMS
# ============================================================================

[docker_context]
symbol = "🐳 "
format = "via [$symbol$context]($style) "
style = "bold blue"
only_with_files = true

[kubernetes]
symbol = "⎈ "
format = 'on [$symbol$context( \($namespace\))]($style) '
style = "bold cyan"
disabled = false

[aws]
symbol = "☁️ "
format = 'on [$symbol($profile )(\($region\) )]($style)'
style = "bold yellow"

[terraform]
symbol = "💠 "
format = "via [$symbol$workspace]($style) "
style = "bold purple"

# ============================================================================
# PACKAGE MANAGERS
# ============================================================================

[package]
symbol = "📦 "
format = "is [$symbol$version]($style) "
style = "bold cyan"
display_private = false

# ============================================================================
# BATTERY (for laptops)
# ============================================================================

[battery]
full_symbol = "🔋"
charging_symbol = "⚡"
discharging_symbol = "💀"
unknown_symbol = "❓"
empty_symbol = "🪫"

[[battery.display]]
threshold = 10
style = "bold red"

[[battery.display]]
threshold = 30
style = "bold yellow"

[[battery.display]]
threshold = 100
style = "bold green"

# ============================================================================
# TIME - Show current time
# ============================================================================

[time]
disabled = false
format = '🕐 [$time]($style) '
style = "bold white"
time_format = "%T"
utc_time_offset = "local"

# ============================================================================
# COMMAND DURATION - How long commands take
# ============================================================================

[cmd_duration]
min_time = 500
format = "took [$duration](bold yellow) "
show_milliseconds = false
show_notifications = false

# ============================================================================
# MEMORY USAGE
# ============================================================================

[memory_usage]
disabled = false
threshold = 75
symbol = "🧠 "
format = "via $symbol[$ram( | $swap)]($style) "
style = "bold dimmed white"

# ============================================================================
# STATUS - Exit code of last command
# ============================================================================

[status]
style = "bold red"
symbol = "🔴"
success_symbol = "🟢"
format = '[\[$symbol $common_meaning$signal_name$maybe_int\]]($style) '
map_symbol = true
disabled = false

# ============================================================================
# CUSTOM MODULES - Add your own!
# ============================================================================

# Example: Show a gaming quote
[custom.gaming]
command = "echo '🎮'"
when = "true"
format = "[$output]($style) "
style = "bold magenta"
disabled = true

# Example: Show if in a git repo
[custom.git_scope]
command = "git rev-parse --show-toplevel 2>/dev/null | xargs basename"
when = "git rev-parse --is-inside-work-tree 2>/dev/null"
format = "[\\[](bold cyan)[$output](bold yellow)[\\]](bold cyan) "
style = "bold yellow"
disabled = true