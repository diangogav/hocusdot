# ============================================================================
# 🎨 PIXEL ART / 8-BIT RETRO STARSHIP THEME 🎨
# ============================================================================
# Inspired by: Super Mario, Zelda, Minecraft, Terraria, Stardew Valley
# Style: ASCII blocks, 8-bit colors, retro gaming aesthetics
# ============================================================================

# Timeout for commands (in milliseconds)
command_timeout = 1000

# Add blank line between prompts
add_newline = true

# ============================================================================
# PROMPT FORMAT - Pixel Art Style
# ============================================================================
format = """
[▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄▄](bold blue)\
[█](bold blue)$username$hostname$directory$git_branch$git_status[█](bold blue)
[█](bold blue)$nodejs$python$rust$golang$docker_context$kubernetes[█](bold blue)
[█](bold blue)$custom.nodejs_version$custom.ollama$custom.scripts[█](bold blue)
[▀▀▀](bold blue)$character"""


# Alternative compact format (uncomment to use)
# format = \"\"\"
# [▓▒░](bold green)$directory$git_branch$git_status
# [▓▒░](bold green)$character\"\"\"

# ============================================================================
# PROMPT CHARACTER - 8-bit style
# ============================================================================
[character]
success_symbol = "[▶](bold green)"
error_symbol = "[✖](bold red)"
vimcmd_symbol = "[◀](bold yellow)"

# ============================================================================
# USERNAME - Player name
# ============================================================================
[username]
style_user = "bold yellow"
style_root = "bold red"
format = "[$user](bold yellow)[◆](bold white)"
disabled = false
show_always = true

# ============================================================================
# HOSTNAME - Server/World
# ============================================================================
[hostname]
ssh_only = false
format = "[$hostname](bold cyan)[◆](bold white)"
disabled = false

# ============================================================================
# DIRECTORY - Current location
# ============================================================================
[directory]
truncation_length = 3
truncation_symbol = "…/"
home_symbol = "🏠"
read_only = "🔒"
style = "bold magenta"
format = "[$path]($style)[$read_only]($read_only_style) "

[directory.substitutions]
"Documents" = "📄"
"Downloads" = "📥"
"Music" = "🎵"
"Pictures" = "🖼️"
"code" = "💾"
"projects" = "🎮"
".dotfiles" = "⚙️"

# ============================================================================
# GIT - Version control with pixel style
# ============================================================================
[git_branch]
symbol = "⎇ "
format = "[on](bold white) [$symbol$branch]($style) "
style = "bold green"
truncation_length = 20
truncation_symbol = "…"

[git_status]
format = '([\[$all_status$ahead_behind\]]($style) )'
style = "bold yellow"
conflicted = "⚔ "
ahead = "⬆${count} "
behind = "⬇${count} "
diverged = "⬍⬆${ahead_count}⬇${behind_count} "
up_to_date = "✓ "
untracked = "?${count} "
stashed = "📦${count} "
modified = "~${count} "
staged = "+${count} "
renamed = "»${count} "
deleted = "✖${count} "

[git_commit]
commit_hash_length = 7
format = "[\\($hash$tag\\)]($style) "
style = "bold cyan"
tag_symbol = "🏷 "
tag_disabled = false

# ============================================================================
# PROGRAMMING LANGUAGES - 8-bit icons
# ============================================================================

[nodejs]
symbol = "⬢ "
format = "via [$symbol$version]($style) "
style = "bold green"
detect_extensions = ["js", "mjs", "cjs", "ts", "mts", "cts"]
detect_files = ["package.json", ".node-version", ".nvmrc"]

[python]
symbol = "🐍 "
format = 'via [$symbol$version]($style) '
style = "bold yellow"
pyenv_version_name = false
detect_extensions = ["py"]

[rust]
symbol = "⚙ "
format = "via [$symbol$version]($style) "
style = "bold red"
detect_extensions = ["rs"]

[golang]
symbol = "🔷 "
format = "via [$symbol$version]($style) "
style = "bold cyan"
detect_extensions = ["go"]

[java]
symbol = "☕ "
format = "via [$symbol$version]($style) "
style = "bold red"

[php]
symbol = "🐘 "
format = "via [$symbol$version]($style) "
style = "bold purple"

# ============================================================================
# TOOLS & PLATFORMS - Pixel style
# ============================================================================

[docker_context]
symbol = "🐳 "
format = "via [$symbol$context]($style) "
style = "bold blue"
only_with_files = true

[kubernetes]
symbol = "☸ "
format = 'on [$symbol$context]($style) '
style = "bold cyan"
disabled = false

[aws]
symbol = "☁ "
format = 'on [$symbol$profile]($style) '
style = "bold yellow"

[terraform]
symbol = "💠 "
format = "via [$symbol$workspace]($style) "
style = "bold purple"

# ============================================================================
# PACKAGE MANAGERS
# ============================================================================

[package]
symbol = "📦 "
format = "[$symbol$version]($style) "
style = "bold cyan"
display_private = false

# ============================================================================
# BATTERY - Health bar style
# ============================================================================

[battery]
full_symbol = "█████"
charging_symbol = "⚡"
discharging_symbol = "▼"
unknown_symbol = "?"
empty_symbol = "▁▁▁▁▁"

[[battery.display]]
threshold = 10
style = "bold red"
discharging_symbol = "▁▁▁▁▁"

[[battery.display]]
threshold = 30
style = "bold yellow"
discharging_symbol = "▂▂▂▁▁"

[[battery.display]]
threshold = 50
style = "bold yellow"
discharging_symbol = "▃▃▂▂▁"

[[battery.display]]
threshold = 70
style = "bold green"
discharging_symbol = "▄▄▃▂▁"

[[battery.display]]
threshold = 100
style = "bold green"
discharging_symbol = "█████"

# ============================================================================
# TIME - Pixel clock
# ============================================================================

[time]
disabled = false
format = "[$time](bold white)"
time_format = "%H:%M"
style = "bold white"

# ============================================================================
# CUSTOM MODULES - System Info
# ============================================================================

# Node.js version (via fnm)
[custom.nodejs_version]
command = "node --version 2>/dev/null | sed 's/v//'"
when = "command -v node"
format = "[⬢ $output](bold green)"
description = "Current Node.js version"

# Ollama status
[custom.ollama]
command = "pgrep -x ollama >/dev/null && echo '🤖 ON' || echo ''"
when = "command -v ollama"
format = "[$output](bold cyan)"
description = "Ollama AI status"

# Scripts count
[custom.scripts]
command = "find ~/.dotfiles/scripts -type f -executable 2>/dev/null | wc -l"
when = "test -d ~/.dotfiles/scripts"
format = "[📜 $output](bold yellow)"
description = "Available scripts count"

# ============================================================================
# COMMAND DURATION - Timer
# ============================================================================

[cmd_duration]
min_time = 500
format = "took [⏱ $duration]($style) "
show_milliseconds = false
style = "bold yellow"

# ============================================================================
# MEMORY USAGE - Health bar
# ============================================================================

[memory_usage]
disabled = false
threshold = 75
symbol = "🧠 "
format = "[$symbol$ram]($style) "
style = "bold red"

# ============================================================================
# STATUS - Game over / Victory
# ============================================================================

[status]
style = "bold red"
symbol = "✖"
success_symbol = "✓"
format = '[\[$symbol $common_meaning\]]($style) '
map_symbol = true
disabled = false

# ============================================================================
# CUSTOM MODULES - Pixel art decorations
# ============================================================================

# Pixel heart (uncomment to show)
[custom.heart]
command = "echo '♥'"
when = "true"
format = "[$output](bold red) "
style = "bold red"
disabled = true

# Coin counter (example)
[custom.coins]
command = "echo '⭐'"
when = "true"
format = "[$output](bold yellow) "
disabled = true

# Level indicator (example)
[custom.level]
command = "echo 'LV.99'"
when = "true"
format = "[$output](bold cyan) "
disabled = true